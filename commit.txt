feat(query+enrich): generic semantic search, Phase 2d/2e enrichment, code quality

Add MetaKG.query() for all-kinds semantic search, fixing metabokg query
and Streamlit search returning empty results for compound/enzyme terms.
Extend enrichment pipeline with glycan (Phase 2d) and KO enzyme (Phase 2e)
phases. Add metabokg query CLI command. Fix quiet-param bug in
enrich_reactions_from_graph. Add authorship/license headers to all
modified modules. Simplify _get_node_label to regex-based bare ID check.

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
